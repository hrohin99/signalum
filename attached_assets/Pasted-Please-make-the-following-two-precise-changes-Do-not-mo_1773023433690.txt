Please make the following two precise changes. Do not modify auth.tsx. Do not modify any other files beyond the two specified.

CHANGE 1: client/src/pages/OnboardingPage.tsx
Add a state variable to track per-entity website URLs. Find:
tsconst [isCreating, setIsCreating] = useState(false);
Add immediately after:
tsconst [entityWebsiteUrls, setEntityWebsiteUrls] = useState<Record<string, string>>({});
In the confirm step, find the entity badges section:
tsx{category.entities.map((entity) => (
  <Badge key={entity.name} variant="secondary" className="text-xs">
    <Tag className="w-3 h-3 mr-1" />
    {entity.name}
    <span className="ml-1 text-muted-foreground">· {entity.type}</span>
  </Badge>
))}
Replace with:
tsx{category.entities.map((entity) => (
  <div key={entity.name} className="flex flex-col gap-1">
    <Badge variant="secondary" className="text-xs w-fit">
      <Tag className="w-3 h-3 mr-1" />
      {entity.name}
      <span className="ml-1 text-muted-foreground">· {entity.type}</span>
    </Badge>
    {entity.topic_type === "competitor" && (
      <Input
        placeholder={`${entity.name}'s website (optional — improves accuracy)`}
        value={entityWebsiteUrls[entity.name] || ""}
        onChange={(e) =>
          setEntityWebsiteUrls((prev) => ({ ...prev, [entity.name]: e.target.value }))
        }
        className="text-xs h-7 ml-1"
      />
    )}
  </div>
))}
In handleConfirm, find:
tsawait apiRequest("POST", "/api/workspace", {
  userId: user.id,
  categories: extraction.categories,
  cityCountry: cityCountry.trim() || undefined,
  websiteUrl: websiteUrl.trim() || undefined,
});
Replace with:
tsconst categoriesWithWebsites = extraction.categories.map((cat: any) => ({
  ...cat,
  entities: cat.entities.map((entity: any) => ({
    ...entity,
    website_url: entityWebsiteUrls[entity.name]?.trim() || undefined,
    disambiguation_confirmed: entity.topic_type === "competitor" && entityWebsiteUrls[entity.name]?.trim() ? true : undefined,
  })),
}));

await apiRequest("POST", "/api/workspace", {
  userId: user.id,
  categories: categoriesWithWebsites,
  cityCountry: cityCountry.trim() || undefined,
  websiteUrl: websiteUrl.trim() || undefined,
});

CHANGE 2: server/routes.ts
In the /api/add-entity handler, find this block:
tsconst newEntity: ExtractedEntity = { name: entityName, type: safeEntityType, topic_type: safeTopicType, related_topic_ids: [], priority: 'medium' };

const tenantId = "00000000-0000-0000-0000-000000000000";
const inferenceResult = await performSiblingInference(entityName, tenantId, { categories }, categoryName, userId);
Replace with:
tsconst incomingWebsiteUrl = (req.body.website_url && typeof req.body.website_url === "string" && req.body.website_url.trim()) ? req.body.website_url.trim() : null;

const newEntity: ExtractedEntity = { name: entityName, type: safeEntityType, topic_type: safeTopicType, related_topic_ids: [], priority: 'medium' };

if (incomingWebsiteUrl) {
  newEntity.website_url = incomingWebsiteUrl;
}

const tenantId = "00000000-0000-0000-0000-000000000000";
const inferenceResult = incomingWebsiteUrl ? null : await performSiblingInference(entityName, tenantId, { categories }, categoryName, userId);
Then find:
tsconst aspectApplicableTypes = ["competitor", "account", "technology"];
if (!aspectApplicableTypes.includes(safeTopicType)) {
  newEntity.disambiguation_confirmed = true;
} else if (inferenceResult) {
Replace with:
tsconst aspectApplicableTypes = ["competitor", "account", "technology"];
if (incomingWebsiteUrl) {
  newEntity.disambiguation_confirmed = true;
} else if (!aspectApplicableTypes.includes(safeTopicType)) {
  newEntity.disambiguation_confirmed = true;
} else if (inferenceResult) {
Do not change anything else. Do not modify any other files.